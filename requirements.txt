Flask==2.3.3
Werkzeug==2.3.7
Jinja2==3.1.2

# Document parsing
python-docx==0.8.11
openpyxl==3.1.2
PyPDF2==3.0.1
python-pptx==0.6.21

# AI / ML
google-genai>=1.0.0
# sentence-transformers removed — too large for free-tier hosting (300MB+)
# RAG engine falls back to TF-IDF automatically
scikit-learn>=1.3.0

# Environment
python-dotenv==1.0.0

# Security
flask-limiter==3.5.0

# Production WSGI server (Railway / Render)
gunicorn==21.2.0

# Utilities
requests==2.31.0
